from setuptools import find_packages, setup

setup(
    name = "mab",
    packages = find_packages(),
    version = "0.0",
    description = "in dev best arm identifier",
    install_requires = ["pomdp-py"],
    author = "justinchiu",
    license = "MIT",
)
